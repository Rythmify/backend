// ============================================================
// models/web-profile.model.js — PostgreSQL queries for Web_Profile
// Entity attributes: Web_profile_id, User_Id, Platform, URL
// All SQL lives HERE — no SQL outside models/
// ============================================================
const db = require('../config/db');

// TODO: Implement query methods
// Example:
// exports.findById = async (id) => {
//   const { rows } = await db.query('SELECT * FROM web-profiles WHERE id = $1', [id]);
//   return rows[0] || null;
// };
