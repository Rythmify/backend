// ============================================================
// models/subscription-plan.model.js — PostgreSQL queries for Subscription_Plan
// Entity attributes: Subscription_plan_id, Name, Price, Duration, Upload_limit (track_limit, playlist_limit)
// All SQL lives HERE — no SQL outside models/
// ============================================================
const db = require('../config/db');

// TODO: Implement query methods
// Example:
// exports.findById = async (id) => {
//   const { rows } = await db.query('SELECT * FROM subscription-plans WHERE id = $1', [id]);
//   return rows[0] || null;
// };
