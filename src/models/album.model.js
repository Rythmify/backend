// ============================================================
// models/album.model.js — PostgreSQL queries for Album
// Entity attributes: Album_Id, User_Id, Title, Genre, Description, Cover_image_url, Track_count, Is_Public, Release_date, Created_at
// All SQL lives HERE — no SQL outside models/
// ============================================================
const db = require('../config/db');

// TODO: Implement query methods
// Example:
// exports.findById = async (id) => {
//   const { rows } = await db.query('SELECT * FROM albums WHERE id = $1', [id]);
//   return rows[0] || null;
// };
