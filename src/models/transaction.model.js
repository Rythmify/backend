// ============================================================
// models/transaction.model.js — PostgreSQL queries for Transactions
// Entity attributes: Transaction_id, User_Subscription_id, Amount, Payment_method, Payment_status, Paid_at, Created_at
// All SQL lives HERE — no SQL outside models/
// ============================================================
const db = require('../config/db');

// TODO: Implement query methods
// Example:
// exports.findById = async (id) => {
//   const { rows } = await db.query('SELECT * FROM transactions WHERE id = $1', [id]);
//   return rows[0] || null;
// };
