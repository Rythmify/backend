// ============================================================
// models/comment.model.js — PostgreSQL queries for Comments
// Entity attributes: Comment_id, User_Id, Track_Id, Content, Track_timestamp, Like_count, Created_at
// All SQL lives HERE — no SQL outside models/
// ============================================================
const db = require('../config/db');

// TODO: Implement query methods
// Example:
// exports.findById = async (id) => {
//   const { rows } = await db.query('SELECT * FROM comments WHERE id = $1', [id]);
//   return rows[0] || null;
// };
