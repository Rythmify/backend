// ============================================================
// models/report.model.js — PostgreSQL queries for Report
// Entity attributes: id, Reporter_Id, Target_User_Id, Track_Id, resource_type, resource_id, reason, description, Status, Created_at
// All SQL lives HERE — no SQL outside models/
// ============================================================
const db = require('../config/db');

// TODO: Implement query methods
// Example:
// exports.findById = async (id) => {
//   const { rows } = await db.query('SELECT * FROM reports WHERE id = $1', [id]);
//   return rows[0] || null;
// };
