// ============================================================
// models/user-subscription.model.js — PostgreSQL queries for User_Subscriptions
// Entity attributes: User_Subscription_id, User_Id, Plan_Id, Status, Auto_renew, Start_date, End_date
// All SQL lives HERE — no SQL outside models/
// ============================================================
const db = require('../config/db');

// TODO: Implement query methods
// Example:
// exports.findById = async (id) => {
//   const { rows } = await db.query('SELECT * FROM user-subscriptions WHERE id = $1', [id]);
//   return rows[0] || null;
// };
