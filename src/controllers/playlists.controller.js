// ============================================================
// controllers/playlists.controller.js
// Owner : Alyaa Mohamed (BE-4)
// Receives validated requests → calls service → returns HTTP response
// ============================================================
const playlistsService = require('../services/playlists.service');
const { success, error } = require('../utils/api-response');

// TODO: Implement controller methods
// Example:
// exports.getAll = async (req, res) => {
//   const data = await playlistsService.getAll(req.query);
//   return success(res, data);
// };
